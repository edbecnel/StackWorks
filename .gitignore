node_modules/
dist/

# Local env overrides (secrets, personal toggles)
.env.local

# Server persistence data (local-only)
server/data/

# Local-only Neo piece PNGs (not bundled/committed)
public/pieces/neo/*.png
