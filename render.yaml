services:
  - type: web
    name: stackworks-server
    runtime: node
    rootDir: server
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: LASCA_DATA_DIR
        value: /var/data/stackworks/games
      - key: LASCA_ADMIN_TOKEN
        sync: false
